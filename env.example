# ============================================================
# Seetara Website - Environment Variables
# ============================================================
# Rename this file to .env.local and fill in your values
# NEVER commit .env.local to version control!
# ============================================================

# ===== Supabase Configuration =====
# IMPORTANT: These should be the SAME as the ERP project!
# Get these from your Supabase project settings > API
# These are safe to expose to browser (anon key has RLS protection)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ===== Cloudflare R2 Storage Configuration =====
# For image uploads - get these from Cloudflare Dashboard > R2
# 🔒 PRIVATE: These are server-side only (NOT exposed to browser)
R2_ACCOUNT_ID=your-cloudflare-account-id
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
R2_BUCKET_NAME=seetara-assets

# Public URL for accessing uploaded files (safe to expose)
NEXT_PUBLIC_R2_PUBLIC_URL=https://pub-your-bucket-id.r2.dev

# ===== Application Settings =====
# Base URL for the website
NEXT_PUBLIC_APP_URL=http://localhost:3001

# ============================================================
# 🔒 SECURE SERVER-SIDE ONLY VARIABLES
# ============================================================
# These are NEVER exposed to the browser for security!

# Aakash SMS API Token
# Get your auth token from https://sms.aakashsms.com/user/dashboard
# 🔒 PRIVATE: Server-side only - NOT prefixed with NEXT_PUBLIC_
AAKASH_SMS_TOKEN=your-aakash-sms-auth-token-here

# Sales Team Phone Numbers (comma separated, 10 digits without +977)
# 🔒 PRIVATE: Server-side only
SALES_NUMBERS=9802359033,9802359035,9802200110

# Google Apps Script URL (for saving orders to Google Sheets)
# 🔒 PRIVATE: Server-side only
GOOGLE_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec

# ============================================================
# PUBLIC VARIABLES (safe to expose to browser)
# ============================================================

# WhatsApp Business Number (include country code without +)
NEXT_PUBLIC_WHATSAPP_NUMBER=9779802359033

# Facebook Messenger Page ID
NEXT_PUBLIC_MESSENGER_PAGE_ID=368155539704608

# ============================================================
# SECURITY NOTES
# ============================================================
#
# ✅ SAFE to expose (NEXT_PUBLIC_ prefix):
#    - Supabase URL and Anon Key (protected by RLS)
#    - R2 Public URL (read-only)
#    - WhatsApp/Messenger IDs (public info)
#
# 🔒 MUST BE PRIVATE (NO prefix):
#    - AAKASH_SMS_TOKEN (can be misused to send SMS)
#    - SALES_NUMBERS (privacy concern)
#    - GOOGLE_SCRIPT_URL (can be spammed)
#    - R2 Secret Keys (can modify storage)
#
# These private variables are only accessible server-side
# via process.env in API routes and server components.
#
# ============================================================
# DEPLOYMENT NOTES
# ============================================================
# For Vercel deployment:
# 1. Go to your Vercel project settings
# 2. Navigate to Environment Variables
# 3. Add all variables (private ones without NEXT_PUBLIC_)
# 4. IMPORTANT: Use the SAME Supabase credentials as the ERP
#
# Required for production:
# - NEXT_PUBLIC_SUPABASE_URL
# - NEXT_PUBLIC_SUPABASE_ANON_KEY
# - AAKASH_SMS_TOKEN
# - GOOGLE_SCRIPT_URL
#
# ============================================================
